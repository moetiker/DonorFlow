# Database - use relative path (works with current/ structure)
DATABASE_URL="file:./prisma/prod.db"

# NextAuth
NEXTAUTH_URL=https://gönner.müüs.ch
NEXTAUTH_SECRET=change-this-to-a-random-secret-in-production

# App Settings
NODE_ENV=production
